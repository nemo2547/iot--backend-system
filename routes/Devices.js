const dbcConfig = {
    host: "localhost",
    port: 3306,
    database: "mydatabase",
    status: "active"
};

console.log("Database Configuration loaded successfully.");

module.exports = dbcConfig;